{
  "name": "xiaoshuo-yijian-huanming",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:safari-first",
    "build:safari-first": "node scripts/build.mjs",
    "build:android-runtime": "node scripts/build-android-runtime.mjs",
    "prepare:chromium-load": "node scripts/prepare-manual-load.mjs",
    "chromium:open": "open -a \"Google Chrome\" \"chrome://extensions/\"",
    "chromium:ready": "npm run build:safari-first && npm run chromium:open",
    "apple:generate": "node scripts/apple-project.mjs generate",
    "apple:sync": "node scripts/apple-project.mjs sync",
    "apple:build:ios": "xcodebuild -project \"apple/小说一键换名/小说一键换名.xcodeproj\" -scheme \"小说一键换名 (iOS)\" -destination \"generic/platform=iOS Simulator\" -derivedDataPath \"build/xcode-ios\" CODE_SIGNING_ALLOWED=NO build",
    "apple:build:macos": "xcodebuild -project \"apple/小说一键换名/小说一键换名.xcodeproj\" -scheme \"小说一键换名 (macOS)\" -destination \"platform=macOS\" -derivedDataPath \"build/xcode-macos\" CODE_SIGNING_ALLOWED=NO build",
    "apple:verify": "npm test && npm run apple:sync && npm run apple:build:ios && npm run apple:build:macos",
    "android:test": "./android/gradlew -p android :app:testDebugUnitTest",
    "android:assemble:debug": "./android/gradlew -p android :app:assembleDebug",
    "testflight:preflight": "node scripts/testflight-preflight.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vitest": "^2.1.9"
  }
}
